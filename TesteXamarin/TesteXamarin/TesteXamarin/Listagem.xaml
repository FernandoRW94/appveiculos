<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TesteXamarin.Listagem">
    <ContentPage.Content>

        <ListView x:Name="lista" ItemTapped="ItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>

                        <StackLayout Orientation="Horizontal" Margin="20, 0, 0, 0">
                            <Label Text="{Binding Fabricante}" VerticalOptions="CenterAndExpand"></Label>
                            <Label Text="{Binding Modelo}" VerticalOptions="CenterAndExpand"></Label>
                        </StackLayout>
                        
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
    </ContentPage.Content>
</ContentPage>